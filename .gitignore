node_modules/
dist/
*.tsbuildinfo
coverage/
# pnpm pack output (the tarball-contents test cleans up after itself; this
# guards a failed/interrupted run from leaving one staged).
*.tgz
.DS_Store
.env*
!.env.example
*.pem
*.key

# local scratchpad — not for public consumption
NOTES.md

# Claude Code per-user local settings (machine paths, permission rules) — never commit
.claude/settings.local.json
