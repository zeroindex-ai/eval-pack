// Barrel for @zeroindex-ai/eval-pack core.
// Public API surface grows as work-list items 3–7 land
// (schema, checks, citations, passRule, runner).

export { recallAtK, percentile, p50, p95 } from './metrics.js';
